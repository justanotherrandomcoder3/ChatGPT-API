import socket

class chatquery(striquery):  
    while True:
        striquery = input("What do you need assistance with?")
 client = socket.socket(www.google.com)
print(socket.listen(client + "/" + striquery));

class SafeSearch:
    while True:     
         if chatquery: SafeSearch[chatquery(SafeSearch)];
opquery = input("What do you need assistance with?")
client = socket.socket(www.google.com)
socket.socket(client)
safemode = ("/safe=active")
print(socket.listen(client + "/" + opquery + safemode))


while True:
        chatquery: SafeSearch[chatquery(SafeSearch)]
